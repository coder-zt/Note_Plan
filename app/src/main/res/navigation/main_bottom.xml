<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto" android:id="@+id/main_bottom"
    app:startDestination="@id/noteFragment">

    <fragment
        android:id="@+id/noteFragment"
        android:name="com.example.noteplan.ui.fragment.NoteFragment"
        android:label="NoteFragment" >
        <action
            android:id="@+id/action_noteFragment_to_planFragment"
            app:destination="@id/planFragment" />
    </fragment>

    <fragment
        android:id="@+id/planFragment"
        android:name="com.example.noteplan.ui.fragment.PlanFragment"
        android:label="NoteFragment" >
        <action
            android:id="@+id/action_planFragment_to_noteFragment"
            app:destination="@id/noteFragment" />
    </fragment>
</navigation>